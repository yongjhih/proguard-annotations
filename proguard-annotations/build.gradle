buildscript {
    repositories {
        jcenter()
    }
}

repositories {
    jcenter()
}

apply plugin: 'java'

dependencies {
}

version = VERSION_NAME
group = GROUP

apply from: 'mvn-push.gradle'
